# Public / third-party sample fixtures

Small files copied from vendored tool trees (CaDiCaL tests, ABC examples)
for differential smoke testing. Not a full competition mirror.

Sources:
- CaDiCaL: `third_party/cadical/test/cnf/`
- ABC: `third_party/abc/i10.aig` when present

Prefer `corpus/edge/` for hand-written extremes and `corpus/bench/` for
scoreboard suites.
